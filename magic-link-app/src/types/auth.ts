export interface TokenData {
  email: string;
  createdAt: number;
}

export interface SessionData {
  email: string;
  createdAt: number;
}

export interface UserInfo {
  email: string;
  name: string;
  lastLogin: string;
}
